# Mapbox Public Access Token
# IMPORTANT: This must be a PUBLIC token (starts with pk.)
# NOT the secret backend token (starts with sk.)
# Get your public token from: https://account.mapbox.com/access-tokens/
MAPBOX_PUBLIC_TOKEN=pk.xxxxxxxx

# API Base URL
# IMPORTANT: For physical devices, replace with your computer's IP address
# Find your IP: Windows (ipconfig) or Mac/Linux (ifconfig)
# Example: API_BASE_URL=http://192.168.1.100:3000/api
# Leave empty to use platform defaults (localhost for emulators)
API_BASE_URL=
